# Lessons

> The compounding asset. `/retro` appends dated, tagged entries: *situation → what we did → what we'd do differently → rule going forward*. Rules recurring 3×+ get promoted into `CLAUDE.md`. Prune past ~150 lines by consolidating.

<!-- newest first -->

## Workflow friction

<!-- Gates always waived, template sections always empty — proposals to change the workflow template, decided deliberately, never auto-applied. -->
