# Meta Pixel ID from Events Manager (business.facebook.com/events_manager2).
# Optional: the pixel only loads when this is set. Set it in Vercel → Project
# Settings → Environment Variables (Production) and redeploy; leave unset for
# local dev. Pixel IDs are public by design (they ship in page source).
VITE_META_PIXEL_ID=
